apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: aegisflow
  labels:
    app: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      # SEEDED FLAW #10: privileged pod, no resource limits.
      # Expected detector: Checkov CKV_K8S_16 (privileged), CKV_K8S_10/11/12/13
      # (missing requests/limits). A privileged pod has full access to the
      # host's devices and kernel capabilities — a container escape here is a
      # node compromise, not just a pod compromise. No resource limits means
      # one runaway pod can starve every other workload on the node.
      automountServiceAccountToken: false
      containers:
        - name: auth-service
          image: ghcr.io/notnaveen222/aegis-flow-auth-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          securityContext:
            privileged: true
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: aegisflow-secrets
                  key: auth-database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: aegisflow-secrets
                  key: jwt-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: aegisflow
spec:
  selector:
    app: auth-service
  ports:
    - port: 8000
      targetPort: 8000
